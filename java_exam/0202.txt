Department Store
문자열 리터럴을 앞에다 쓰면 NullPointerException을 발생시키지 않으므로, 
grade.eqauls("VVIP") 로 refactoring 하는 것이 좋음
NullPointerException을 "회피"하지 말고 "제어"해라.
위 처럼 쓸꺼면 null check를 선행하던지, try-catch로 NullPointerException을 감지 하는게 좋은데,
위의 방식에서 try-catch를 쓰면 코드가 너저분해져서 grade.equals("VVIP"); 로 쓰는게 제일 깔끔할 듯 싶다.

동훈님 코드에서
추상클래스 생성자가 어색하다고 생각해서 질문을 했다.
나는 마켓, 편의점 같은 이름을 굳이 추상클래스 까지 가져가서 사용해야 되나? 그런 생각을 했다.
결국 마켓, 편의점 안에서 사용되는건데,,..
그런데 생각을 달리 해보면 이름이 없는 마켓은 없다. 그러니까 
추상클래스에서 생성하는 것이 괜찮은것같다.
공통으로 관리하는 인스턴스 필드는 추상클래스 또는 인터페이스에서 관리하고,
각각의 클래스 마다 구조가 다른 것들 (추가적으로 필요한 인스턴스 필드, 클래스 필드)은 하위 클래스에서 각각 관리하는 것이 좋을듯싶다.

예외 처리
똑같은 기능을 하는 예외 처리면 또는으로 처리하는게 낫다 (catch 한번만)
Exception e 를 마지막으로 선언해주는건 좋지 않다.

System.arraycopy()

제네릭 (Strategy Pattern)
제네릭에서의 익명클래스 사용

무조건 배열 대신에 ArrayList 컬렉션을 권장하지만,
String.split 같은 반환 타입이 배열인 경우에는 어쩔수 없이 배열을 사용한다.

Map 컬렉션은 Hashing을 사용해서 반복하는게 의미없음